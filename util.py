from collections import OrderedDict

def key_value_parase(raw_data, start = 0, dct = None):

    if not dct:
        dct = OrderedDict()

    spc = raw_data.find()
    

def key_value_serialize():
    pass